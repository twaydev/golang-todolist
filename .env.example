# Server Configuration
PORT=8080
ENV=development
LOG_LEVEL=info

# Database (PostgreSQL/Supabase)
# For local development with Docker Compose, use:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5433/todolist?sslmode=disable
# For production/Supabase:
DATABASE_URL=postgresql://user:password@localhost:5432/todolist?sslmode=disable

# JWT Authentication
JWT_SECRET=your-secret-key-minimum-32-characters-long-change-in-production
JWT_EXPIRY_HOURS=24

# Railway (auto-set by Railway platform)
RAILWAY_ENVIRONMENT=
RAILWAY_PUBLIC_DOMAIN=

# ─────────────────────────────────────────────────────────────────────────────
# Docker Compose Local Development
# ─────────────────────────────────────────────────────────────────────────────
# These values are used by docker-compose.yml (already configured there)
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_DB=todolist
# PostgreSQL is exposed on port 5433 to avoid conflicts with local installations

# ─────────────────────────────────────────────────────────────────────────────
# MCP Integration Credentials (Required for Orchestrator)
# ─────────────────────────────────────────────────────────────────────────────

# Linear - Task management and progress tracking
# Get API key from: https://linear.app/settings/api
LINEAR_API_KEY=lin_api_xxxxx

# Supabase - Database operations and migrations
# Get from: Project Settings > API
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_KEY=eyJxxxxx

# Railway - Deployment and environment management
# Get from: Account Settings > Tokens
RAILWAY_TOKEN=xxxxx
